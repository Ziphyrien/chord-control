# 0.4.7

- 主程序更新记录下载验证、安装包暂存、进程退出准备和安装器启动阶段，包含检查关联 ID、进程 ID、版本与时间。
- 安装器保存运行时检查、维护退出、文件替换、设置写入及程序启动结果；日志在主程序退出后仍保留。
- 新程序记录启动完成或失败，便于区分安装器启动请求与实际启动状态。
- 安装目录新增只读诊断采集脚本，一次导出相关日志、进程、版本、安装包哈希和系统事件，保留现场后再重试。
