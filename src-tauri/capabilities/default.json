{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Only the main control window can use app IPC and manage login startup",
  "windows": ["main"],
  "permissions": ["core:default", "autostart:default"]
}
