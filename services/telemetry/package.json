{
  "name": "chord-telemetry-server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:worker": "wrangler dev",
    "build": "bun run --cwd ui build",
    "deploy": "bun run build && wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "4.135.0"
  }
}
